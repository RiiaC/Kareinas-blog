npx quartz build --serve --watch
> is the one you need to create the localhost version of the blog, to see changes as they happen (followed by opening http://localhost:8080 in your browser)

npx quartz build
> is the one that replaces everything in the public folder and redoes the blog including any changes since the last time